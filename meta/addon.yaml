name: osac-compute-kubevirt
display_name: Compute (KubeVirt)
description: KubeVirt VM provisioning for OSAC
version: 0.1.0

dependencies:
  osac_core: ">=0.1.0"

resource_types:
  - name: ComputeInstance
    scope: tenant
